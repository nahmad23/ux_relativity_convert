---
# =============================================================================
# roles/uxa_relativity_v2/tasks/service_password.yml
# Placeholder for include_recipe 'uxo_service_password::default'
# (only ran in Chef when node['uxa_relativity_v2']['win_passwordrotation']).
#
# The original logic lived in the external uxo_service_password cookbook, which
# is not part of this cookbook and so is out of scope for this conversion.
# Implement the equivalent rotation here (e.g. ansible.windows.win_user_right /
# win_service credential update) if/when that cookbook is ported.
# =============================================================================

- name: Service password rotation placeholder
  ansible.builtin.debug:
    msg: >-
      win_passwordrotation is enabled but uxo_service_password is an external
      cookbook not included in this conversion. Port it separately.
